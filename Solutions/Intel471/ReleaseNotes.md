| **Version** | **Date Modified (DD-MM-YYYY)** | **Change History**                          |
|-------------|--------------------------------|---------------------------------------------|
| 3.1.0       | 01-09-2026                     |  Updated **Playbook** 'Intel 471 Malware Intelligence to Sentinel' to **v3.1**: no threat type filter is sent any more, so **bulletproof hosting** indicators are ingested alongside malware indicators and the Verity mapping is no longer malware-specific; YARA indicators are labelled `yara` rather than `stix`; the request filters are now recorded alongside the cursor and the cursor is reset automatically when they change, controlled by the new `MigrationMarginHours` parameter; the cursor is stored only after a page has been uploaded and a failed upload now fails the run; indicators with no expiration date no longer fail the run.|
| 3.0.1       | 28-08-2026                     |  Added 25 **Hunting Queries**. <br/> Updated **Playbook** 'Intel 471 Malware Intelligence to Sentinel' to **v3.0**: Key Vault secret names can now be supplied at deployment, and Blob storage now authenticates with the logic app's managed identity - grant it the `Storage Blob Data Contributor` role or every run fails.|
| 3.0.0       | 02-12-2025                     |  Added the Verity 471 backend alongside Titan, and switched indicator ingestion to the Threat Intelligence Upload Indicators API.|